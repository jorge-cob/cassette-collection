import type { Alpine } from 'alpinejs';

export default (Alpine: Alpine) => {
  // Global Alpine.js plugins and stores will be registered here.
  // This entrypoint is loaded eagerly on every page via astro.config.mjs.
};
